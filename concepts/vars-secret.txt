 Mental model 
 variabels are used in cases those are required by multiple wokflow.
 #these are defined ar repo level in Settings → Secrets and variables → Actions → Variables

 Used in workflows as ${{vars.Region}}

 we can use
                 Is this value shared?
                     │
          ┌──────────┴──────────┐
          │                     │
         Yes                   No
          │                     │
       Is it sensitive?    Is it only for
          │                this workflow?
    ┌─────┴─────┐              │
    │           │              │
   Yes         No             Yes
    │           │              │
 secrets      vars           env

 How to decide what to use..?
 I first determine whether the value is sensitive. If it is, I use secrets. 
 If it's non-sensitive and shared across repositories or workflows, I use vars. If it's only relevant to a specific workflow, job, or step, I use env

 Precendence
 Environment Variables
        ↑
Repository Variables
        ↑
Organization Variables

Secrets
Secrets are encrypted values managed by GitHub used to store sensitive information.
Similarl as variables.
but used for secrets.