name: testing
on:
  workflow_dispatch:

env:
  WORKFLOW_VAR: "workflow"

jobs:
  test:
    uses: Deepakj25/Deepakj25/.github/workflows/blank.yml@master
    # uses: Deepakj25/Deepakj25/.github/workflows/blank.yml@v1
    # uses: Deepakj25/Deepakj25/.github/workflows/blank.yml@5d85c97d0d802c72c3894ac53b1339efd4ca62e6
    with:
        enviornment: "Dev"
        enable-logging: true
        replicas: 3
    secrets:
        REPO_SECRET: ${{ secrets.REPO_SECRET }}

  Job2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run Compsosite Action
        uses: ./.github/actions/hello
        with:
          enviornment: "Dev"
